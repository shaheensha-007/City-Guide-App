import 'package:flutter/material.dart';

class Eventsprogrames extends StatefulWidget {
  const Eventsprogrames({super.key});

  @override
  State<Eventsprogrames> createState() => _EventsprogramesState();
}

class _EventsprogramesState extends State<Eventsprogrames> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.indigo,
    );
  }
}
